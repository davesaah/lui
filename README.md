# Local AI RAG UI

A UI to interact with local AI models served with ollama.

## Dependences

- Python 3.11

## How to run

Install dependences

```bash
pip install -r requirements.txt
```

> Make sure your ollama server is running.

Start the UI

```bash
streamlit run app.py
```
